# ===================================================
# alias
# ===================================================

alias=techpoc-version:
  tasks:
  - buildingblock-version
  annotations:
    description: "-> generate version informations (auto called by dobi.sh)"

alias=techpoc-build:
  tasks: 
    - build-torizon-core-docker
  annotations:
    description: "-> build all content"

alias=techpoc-deploy:
  tasks: []
  annotations:
    description: "-> deploy all content (not implemented yet)"

alias=techpoc-test: 
  tasks: []
  annotations: 
    description: "-> test all content (not implemented yet)"